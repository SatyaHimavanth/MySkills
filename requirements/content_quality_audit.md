# Content Quality Audit

The final skill audit checks: cross-file routing, shared/local/prod structure, absence of non-portable citation markup, and durable references from `SKILL.md`.

Run:

```bash
python requirements/content_quality_audit.py
```
